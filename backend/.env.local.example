# 🏑 Hockey Management System - Configuración Local para Testing
# VERCEL DATABASE - Variables de prueba local
# ⚠️  IMPORTANTE: Estas son variables de ejemplo, obtén las reales desde Vercel Dashboard

# Vercel Postgres (Reemplaza con tus valores reales)
POSTGRES_URL="postgres://default:password@hostname:5432/verceldb"
POSTGRES_PRISMA_URL="postgres://default:password@hostname:5432/verceldb?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NO_SSL="postgres://default:password@hostname:5432/verceldb"
POSTGRES_URL_NON_POOLING="postgres://default:password@hostname:5432/verceldb"
POSTGRES_USER="default"
POSTGRES_HOST="hostname-pooler.vercel-postgres.com"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="verceldb"

# Vercel KV (Reemplaza con tus valores reales)
KV_URL="redis://hostname.kv.vercel-storage.com:port"
KV_REST_API_URL="https://hostname.kv.vercel-storage.com"
KV_REST_API_TOKEN="token"
KV_REST_API_READ_ONLY_TOKEN="readonly_token"

# Vercel Blob (Reemplaza con tus valores reales)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_token"

# Configuración de la aplicación
NODE_ENV=production
PORT=3000

# Para desarrollo local (usar cuando se desarrolle en localhost)
# NODE_ENV=development
# DATABASE_URL="postgresql://localhost:5432/hockey_local"
